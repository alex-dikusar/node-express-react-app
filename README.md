# Template NodeJs + ExpressJS + ReactJS application

## Backend side

1. Install Dependencies
   run `npm install`
2. to run app use `npm start`

## Client side

1. Install Dependencies
   run `npm install`
2. to run app use `npm start`
